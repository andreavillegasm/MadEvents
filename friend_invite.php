<?php
require_once 'classes/Database.php';
require 'classes/Guest.php';

//Get the database connection
//$dbconn = Database::getDb();
//$f = new Guest($dbconn);

//if (isset($_POST['addGuest'])) {

    //Add them into the guest list
    //echo $event_id = $_POST['eid'];
    //echo $friend_id = $_POST['fid'];

    //$f->addGuest($event_id, $friend_id);

//}
